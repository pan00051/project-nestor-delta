# Nestor Delta · S7–S10 算法路线与验收计划

> 本文档定义 S7 到 S10 的范围、验收标准与叙事主线。
> 核心原则:**每个 Sprint 的验收都不依赖"预测精度打赢 persistence"。**

---

## 0. 计划的出发点

### 0.1 已确认的缺陷

`relation_weights.py` 的 `_pearson_correlation` 运行在原始水平值上,全仓库无任何平稳化处理
(`diff / detrend / stationary / pct_change` 零命中)。而 case 数据全部是指数与比率序列,
一阶自相关逼近单位根:

| 序列 | level 一阶自相关 | 差分后 |
|---|---|---|
| retail_volume | 0.9477 | 0.0758 |
| unemployment_rate | 0.9981 | 0.8108 |
| industrial_production | 0.9223 | 0.0615 |
| hicp | 0.9925 | 0.1565 |

**验证实验**:两条完全独立的随机游走(n=216,500 seeds),真实答案为"无关系":

| 打分路径 | 中位数 \|r\| | p90 | 通过 0.06 忽略阈值 | 通过 0.50 满信任线 |
|---|---|---|---|---|
| 当前(水平值) | **0.398** | 0.761 | **92.2%** | 显著非零 |
| 差分后 | 0.046 | 0.111 | 39% | 0% |

结论:当前的关系分数主要反映共同趋势,不反映关系。忽略阈值 `0.06` 对伪关系几乎不设防。

### 0.2 由此推出的两条设计约束

**约束 A — 平稳化必须排在稳定性之前。**
趋势驱动的相关性在滚动窗口上极其稳定(两条都在涨的序列,怎么切窗口都在一起涨)。
若在当前 level 分数之上叠加 Temporal Stability 与 Evidence Gate,系统会**优先选中伪关系
并给它们最高置信度**。顺序错了会让 Delta 变得更差且更自信。

**约束 B — 忽略阈值不能是固定常数。**
相关系数的噪声地板是 `1/√n`。西班牙 train 窗口 n=191,噪声地板 0.072,2σ 门槛 0.145。
固定的 `0.06` **低于噪声地板**,即使在差分后的数据上也仍有 39% 的纯噪声能通过。
阈值必须相对噪声地板定义,并对多重 lag 比较做修正。

### 0.3 成功标准的调整(本计划最重要的一条)

**旧标准**:预测精度优于 persistence。
**问题**:月度经季调宏观指数接近随机游走,打赢它是公认难题;且当前 24 点测试窗口的
MAE 比率标准误约 20%,任何个位数效应都不可测量。按此标准迭代会陷入无法收敛的循环。

**新标准**:Delta 是**关系的测谎仪**,回答"这个关系是真的吗",而非"明天涨还是跌"。

此标准下,每个 Sprint 的验收都可做在**正确答案已知的合成 fixture** 上,可跑 N 个种子
给出置信区间,是干净的是/否判定。这是"每个算法叠加都有可验证提升"唯一能成立的前提。

真实数据(Eurostat)的角色随之改变:从**记分牌**变为**诚实行为的演示场**——包括允许
输出"无可用关系,退回 baseline"。这与仓库已有的 validation baseline guard 文化一致。

### 0.4 统一的 Sprint 模式

```
造一个正确答案已知的合成 fixture
  → 验收 = 机制能否恢复正确答案(N/N seeds,报分布而非单点)
  → 再在真实数据上跑一遍,如实报告,包括否定结果
```

---

## S7 · 平稳化层与伪关系检测

**成功率评估:95%** — 验收基于已知 ground truth 的合成数据,不依赖任何真实数据表现。

### 目标
让关系测量测的是关系,不是趋势。

### 范围边界
- 新增并列模块,**不修改** `relation_weights.py` 的既有逻辑。
- 旧路径保留为 `legacy_level_scoring`,作为**对照组**(Fixture A 的演示必须有它在场)。
- S0–S6 全部冻结报告与 SHA-256 原样保留,不作废、不重算、不追溯修改。
- S7 只处理经显式变换后的 short-run dynamic relationships。
- S7 暂不支持 cointegration、ECM/VECM 或 long-run relationships;不得宣称差分保留所有真实关系。
- 不做 stability、不做 gate、不做非线性。

### 交付物
1. `stationarity.py`
   - per-signal 变换声明:`none / diff / log_diff`,写入 case config,显式而非自动猜测。
   - transform declaration 是是否允许打分的决策依据;算法不得根据检验结果自动猜测或替用户选择变换。
   - `lag-1 ACF > 0.95` 仅作为 highly-persistent / non-stationarity risk flag,不是正式平稳性判定。
   - ADF/KPSS 如实现,仅作为 diagnostic 字段或报告诊断,不负责自动选择 transform。
   - 对触发 risk flag 且没有显式 transform 声明的 level scoring,拒绝执行或强制要求声明变换。**拒绝优于静默出错。**
2. `RelationWeight` 增加 `transform` 字段,记录该关系是在哪种变换下测得的。
3. Fixture A / Fixture B(见下)。
4. 全部既有 case 在新路径下重跑,新旧结果并列committed。

### Fixture 规格

**Fixture A — 证明能识别假的**
- 构造:两条独立随机游走,n=216,N=500 seeds,真实答案 = 无关系。
- 期望:旧路径 median \|r\| ≈ 0.40、P(过 0.06) ≈ 92%;新路径 median \|r\| ≈ 0.05、P(\|r\|>0.30) = 0%。

**Fixture B — 证明没把真的杀掉(本 Sprint 的防守要害)**
- 构造:在 x、y 上各叠加线性趋势,真实驱动为 `Δy_t ← 0.6 · Δx_{t-3}`,n=216,N=200 seeds。
- 已验证结果:旧路径 median \|r\| = 0.861(数字更大,但 lag 错误);
  新路径 median \|r\| = 0.503,**100% 的种子恢复出正确 lag=3**。
- 作用:没有 Fixture B,任何人都可以质疑"你只是把信号一起差分掉了"。
  有了它,可以证明修复是**有选择性的**:杀假的、留本 Sprint 范围内的 short-run 真关系、并把 lag 修正。
- 边界:Fixture B 不证明差分会保留所有真实关系;cointegration / long-run relationship 留到 S11+ 或单独 Sprint。

### 验收清单
- [ ] Fixture A:旧路径 median \|r\| ≥ 0.35 且 P(过 0.06 门) ≥ 90%;新路径 median \|r\| ≤ 0.10 且 P(\|r\|>0.30) = 0%。
- [ ] Fixture B:新路径 N/N seeds 恢复正确 lag=3,且 \|r\| 显著超过噪声地板。
- [ ] 四条 Eurostat 序列的 `lag-1 ACF > 0.95` 只触发 highly-persistent / non-stationarity risk flag,报告中不得把它写成正式平稳性判定。
- [ ] ADF/KPSS 如实现,只作为 diagnostic 输出;transform declaration 才是执行打分的决策依据。
- [ ] Spain / dual-window 全部 case 在新路径下重跑,新旧对照 committed,**不挑好看的报**。
- [ ] 旧路径行为逐字节不变(既有测试全绿,旧报告 SHA-256 一致)。

### 已知的真实数据预期
西班牙 train 窗口差分后逐 lag 扫描(噪声地板 2σ = 0.145):

| 信号 | best lag | r | 判定 |
|---|---|---|---|
| industrial_production | 2 | **−0.355** | 显著(\|t\| ≈ 5) |
| consumer_confidence | 5 | +0.153 | 压线,计入多重 lag 修正后不成立 |
| hicp | 2 | +0.153 | 压线,同上 |
| unemployment_rate | 2 | +0.104 | 不显著 |

注意 `industrial_production` 在旧路径上是 **+0.791 @ lag 1**,差分后为 **−0.355 @ lag 2**,
**符号翻转**。S7 的真实数据结论不是"信号归零",而是"唯一那条真关系此前被测反了"。
(需在报告中注明:两条序列均经季节调整,季调程序可能引入负自相关,此为已知 caveat。)

### 故事
> "我发现我自己的关系引擎给一对完全无关的序列打了 0.398 分,而我的忽略阈值是 0.06——
> 92% 的纯噪声能通过。这是我如何发现它、证明它、并在不破坏任何历史结论的前提下修复它。"

与 README 已有的 "The Design Correction That Shaped the Project" 同构,但量级更大。

---

## S8 · 评估分辨率

**成功率评估:90%** — 纯工程改造,无算法风险。

### 目标
让"这次改动带来提升了吗"这个问题**有答案**。

### 问题陈述
当前单次固定切分,test 窗口 24 点,persistence MAE = 0.8042(恰等于平均月度变动,
因为 level 上的 persistence 就是"预测零变化")。MAE 比率的标准误约 **20%**。

对照 dual-window 结论:Case B 验证 +7.11%、疫情窗口 −9.63%。**这两个数都落在噪声内。**
现有报告将其解读为"结构断裂导致关系失效",但以现有样本量,它同样可以只是抽样波动——
当前设计无法区分这两种解释。

### 交付物
1. `rolling_origin.py` — 多折 rolling-origin 切分,取代单折(旧协议冻结为 v1,不删除)。
2. 指标扩展:MASE(尺度无关,便于跨 case 比较)、方向准确率、worst-decile error。
3. **区间报告**:每折结果的分布,而非单点比率。
4. `noise_floor.py` — 给定 n 与 lag 扫描宽度,输出 \|r\| 的显著性门槛(供 S10 的 Gate 复用)。
5. 直接在 Δ 上评估的选项(level MAE 被水平尺度稀释:0.74% 的相对误差看似很小,
   但它 100% 是变动量)。

### 验收清单
- [ ] 同一模型在多折下报出提升区间,而非单点数字。
- [ ] **随机预测器对照**:随机预测器的多折区间必须包含"零提升",否则评估本身有 bug。
- [ ] dual-window 的 +7.11% / −9.63% 在新协议下重新评估,明确结论是否在噪声内。
- [ ] 旧协议 v1 的结论原样保留,新旧并列。

### 故事
> "我原来的评估用 24 个点,标准误 20%。所以我之前报的 7% 提升和 9% 退化,
> 统计上是同一件事。我重建了评估,让后面所有的算法实验第一次真的可以被证伪。"

---

## S9 · 时间稳定性与关系生命周期

**成功率评估:80%** — S4 的 120 行 causal rolling window 已在运行,缺的只是聚合层。

### 目标
把已有的关系轨迹压成可用的稳定性统计量,并建立关系的生老病死。

### 前置依赖
**必须在 S7 之后。** 见约束 A:在未平稳化的分数上算稳定性会主动奖励伪关系。

### 交付物
1. 轨迹聚合:`target_source_trajectory` 的输出 → `stability`(滚动一致性)、
   `uncertainty`(估计量方差 / bootstrap)。
2. `RelationWeight` 扩展为 Relation Object v1:
   ```
   source, target, lag, weight, score, sample_count   # 已存在
   transform                                          # S7 新增
   stability, uncertainty, selected                   # S9 新增
   ```
   **注意:GPT 方案中的 `direction` 已隐含于 `sign(weight)`,`lag` 早在 S2 已实现,
   无需重做。真正的新增只有三个字段。**
3. 生命周期状态机:`birth → strengthening → stable → decaying → dead`。
4. 证据不足时字段置 `null` 而非硬估——null 是信息,垃圾估计不是。

### Fixture 规格
**Fixture C — 关系死亡检测**
- 构造:合成序列中一条真实关系在第 T 步被人为撤除,ground truth 变点已知。
- 验收指标:detection lag(从真实变点到状态转为 `decaying` 的步数)的中位数与分布。

### 验收清单
- [ ] Fixture C:N/N seeds 在 K 步内检测到关系死亡,报 detection lag 分布。
- [ ] **Fixture A 回归测试(关键)**:伪关系在新路径下 stability **不得**虚高。
      这条防止"稳定性机制反过来给伪关系背书"。
- [ ] 拥有大量历史样本的关系,不因样本量大而在关系已衰减时维持高 confidence。
- [ ] S7 的两个 fixture 全绿(无回归)。

### 故事
> "关系不是永恒的。这是一个能看着一条关系诞生、稳定、衰退、死亡的系统,
> 并且不会因为它有十年历史数据就继续相信它。"

---

## S10 · Evidence Gate 与 Prediction Confidence v0

**成功率评估:75%** — 验收用 precision/recall 而非 MAE,绕开了低功效评估的困境。

### 目标
用证据强度取代固定阈值决定选择,并让系统知道自己什么时候不知道。

### 交付物
1. **Evidence Gate v1**,取代固定 `0.06`:
   ```
   Selection = effect size(相对 S8 的噪声地板)
             + stability(S9)
             + sample support
             + 多重 lag / 多重 pair 的 FDR 修正
   ```
   **架构约束(写进 BLUEPRINT,不只是参数选择)**:
   ```
   合法:  relationship uncertainty → selection
   禁止:  prediction error        → selection
   ```
   后者是自激式过拟合:预测错 → 放宽阈值 → 模型容量增加 → 训练误差下降 → 误判为改善。
2. **Prediction Confidence v0**:
   ```
   relation stability + parameter uncertainty + input support(OOD)+ residual uncertainty
   ```
   `input support` 属于 Delta 而非 Insight:Delta 只需说"当前 X 超出我学到 f(X) 的可靠范围",
   解释那个异常环境是什么才是 Insight 的职责。此边界不被破坏。

### Fixture 规格
**Fixture D — 选择质量**
- 构造:已知混合体——若干真关系 + 若干伪关系(随机游走)+ 若干已死关系。
- 验收指标:**precision / recall**,而非 MAE。这是本计划中最干净的一个指标。

### 验收清单
- [ ] Fixture D:Gate 的 precision / recall 显著优于固定阈值基线,报置信区间。
- [ ] **Confidence 校准**:低 confidence 的预测必须有系统性更高的实际误差
      (报分箱校准曲线或 rank correlation)。未校准的 confidence 等于没有 confidence。
- [ ] 架构约束有自动化测试守护:任何把 prediction error 接入 selection 的改动必须导致测试失败。
- [ ] 真实数据上允许并正确输出"无可用关系,退回 baseline"。
- [ ] S7 / S9 全部 fixture 无回归。

### 故事
> "系统知道该相信自己的哪些关系,也知道自己什么时候不该开口。
> 我用一组正确答案已知的混合关系测了它的准确率和召回率——而不是用一个我测不准的 MAE。"

---

## S11+ · 表达能力扩展(条件性)

**仅在 S7–S10 全部通过、且有证据显示仍有空间时启动。**

顺序:非线性增量 → 条件关系 → 频域(FFT / STFT / Welch coherence)。

强制约束:
1. 一律以**增量**形式验收:`NonlinearIncrement = Perf(nonlinear) − Perf(linear)`。
   问题不是"非线性模型拟合得好吗",而是"它解释了线性解释不了的东西吗"。
2. 频域必须先满足数据条件:序列长度需容纳多个 Welch 分段,
   `dominant_frequency` 需 ≥ 5–10 个完整周期。
   **已知陷阱**:单段 periodogram 构造的 magnitude-squared coherence 恒等于 1。
   若未做多段平均即上线,会得到一张 coherence 全部接近 1 的表并误判为重大发现。
3. 不满足条件时字段置 `null`。

---

## 附:关于"打赢 persistence"的诚实预期

S7 之后,西班牙数据上唯一稳超噪声地板的关系是 `industrial_production Δ @ lag 2, r = −0.355`,
约解释 12.6% 的 Δ 方差,理论上可换得个位数的 MAE 改善。
配合 S8 的多折评估,**能否测出统计上可信的提升,估计概率 35–45%。**

这个数字不高。但本计划的全部四个 Sprint 的验收都不依赖它——这正是它们成功率
在 75%–95% 区间的原因,也是"算法不是为了加而乱堆"这个要求能够被真正满足的原因。
