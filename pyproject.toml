[project]
name = "nestor-delta"
version = "0.0.0"
description = "Data-layer evaluation groundwork for Nestor Delta."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[project.optional-dependencies]
web = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
]

[tool.nestor_delta]
evaluation_protocol = "EVALUATION.md"
current_milestone = "M1"
current_sprint = "Sprint 4"
