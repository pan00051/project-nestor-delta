"""Nestor Delta website frontend (Streamlit). Consumes the HTTP API only.

This package must never import `nestor_delta` or recompute any analytic result.
It renders exactly what /snapshot, /audit, and /analyze return.
"""
