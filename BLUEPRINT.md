# Nestor 项目 · 蓝图与规则文档

> **本文件是本项目的唯一权威事实来源(single source of truth)。**
> 作者会定期清理对话窗口。任何一次对话中出现的方向、想法、建议，若与本文件冲突，**一律以本文件为准**。
> 需要改变方向时，先更新本文件，再据此行动，而不是让某次对话里的临时灵感覆盖既定边界。
> 协作 AI(GPT 等)在每次协作开始时应先读本文件，并在文末「协作须知」的约束下工作。

---

## 0. 一句话定义

Nestor 是一个多维数据·事件关系分析系统，由三个可独立交付的作品组成。其最终目的**不是商业产品**，而是**一份可验证的作品集 + 作者的个人爱好完整形态**。

---

## 1. 目标与定位(边界的根基)

- **最终目的是「可验证的作品集」，不是「打赢市场的产品」。** 这条决定了下面一切。
- 因此：**竞品多、无专利级原创性、思路已被研究过，都不构成问题。** 作品集证明的是「作者能否把这件事做出来、做扎实、讲明白」，不是「是否独一无二」或「能否商业化」。
- 作者的真实资产是「独立把 AI 产品从想法做到上线」的执行力。本项目服务于强化这个形象，用于欧洲 AI Product Manager / 数据产品方向求职，并替换简历中一段低价值的无关工作经历。
- **想法(idea)没有护城河，执行(execution)才有价值。** 不为「思路被看到/被借鉴」焦虑；把精力投在做出来和验证上。

---

## 2. 系统结构：三个独立作品

每一块都是一个**能独立停下来、独立写进简历**的完整交付点，不是必须跑到底才算数的连续阶段。

| 代号 | 层 | 内容 | 状态 |
|------|-----|------|------|
| **Nestor Delta** | 数据层 | 多变量之间的关系、权重、动态变化、忽略值等的建模与预测 | 待启动 |
| **Nestor Insight** | 信息层 | 事件影响度评估分析(已有大半)，**仅差「忽略值」模块** | 近乎完成 |
| **Nestor** | 完整版 | 数据层 × 信息层结合；实现「重大变化可回溯到已评估事件」的因果追溯。也是作者个人爱好的完整形态 | 前两项完成前**不动** |

**离终点最近的起点：** 很可能是**给已有的 Nestor Insight 补上「忽略值」模块并收尾**，而非从零搭 Nestor Delta。优先用存量，而不是开新战场。

**命名备注(可安全推迟的决定)：** 「Insight 这个好名字最终归谁」无需现在决定。完整版可能直接叫 Nestor(母品牌)，Insight / Delta 作为其下模块名保留。好名字是可重新分配的资产，不会浪费，不必现在纠结。

---

## 3. 架构原则(不可违背)

1. **模块化、低耦合、高内聚。** 拒绝「所有代码糊成一锅粥、牵一发动全身」。
2. **开闭原则：加新模块不改旧逻辑。** 新东西通过「组合已有模块」实现，而非修改已有模块内部。
3. **区分「能力」与「层」这两个正交维度，这是本架构的核心洞察：**
   - **能力**(通用、与具体层无关)：算关系权重 / 算忽略值 / 让权重动态变化。
   - **层**(调用能力的场景)：数据层 / 信息层 / 跨层。
   - **「动态变化」等能力必须做成一份通用机制，被多处复用**，绝不为每个层各写一遍(否则「改一处要改三处」，直接违背开闭原则)。
4. **第一个要做透的，是被复用最多的底座模块**(通用权重机制)，因为所有上层都依赖它；地基晃则全塌。

---

## 4. 收敛纪律(防膨胀，最重要的一节)

作者反复出现的最大风险是**范围膨胀 + 无限期不收尾**。以下规则用于对抗它：

1. **只允许「容量扩展」，禁止「逻辑膨胀」。** 后期只增加摄入的数据量 / 信息量，**不再把已优化的核心逻辑复杂化**。核心逻辑收敛，容量向外扩展。
2. **不追求把所有权重模块全做完。** 完整愿景里的多个权重(数据关系权重、忽略值、动态变化、信息层各权重、跨层权重等)是**扩展蓝图**，不是交付工作量。作品集只需做透**其中两三个核心**，并**把架构设计成其余可按同一模式扩展**，无需真的全部实现。
3. **区分两种「优化 / 存活」，永远清楚自己在哪一轨：**
   - 「跑得快 / 跑得省」= 工程效率(A 轨性质)。
   - 「想得准 / 判断得对 / 会成长」= 建模能力(B 轨性质，本项目核心追求就在这)。
   - 每冒出一个新想法，先自问它落在哪一边，避免无意识越界。
4. **每一块交付都要有明确的「完成定义」和评估标准**(尤其：预测/判断准不准、如何量化、达到什么程度算做成)。没有终点线的模块不许开工。

---

## 5. 星辰大海(明确划出边界并合上)

以下属于「实现梦想之后」的远期可能，**作者明确决定：现阶段乃至一年内不予考虑，不投入精力**：

- 提纯核心算法逻辑，扩展应用到更多领域 / 场景。
- 任何超出「三个作品」范围的通用化、平台化设想。

**开关规则：** 每当想给项目增加东西时，自问一句：**「这是当前交付，还是星辰大海？」** 若是后者，合上，记入本节，不在当下展开。

---

## 6. 技术取向(避免走弯路的默认判断)

- **不默认核心一定是 LLM。** 数据层的预测内核，很可能用经典时间序列 / 统计学习方法又快又准又省算力；LLM 更适合放在**外围**(结果解释、自然语言报告、对话式切换观测视角)。把 LLM 放在它真正有优势的地方，而非让它去干回归模型干得更好的活。
- **贡献定位诚实：** 作者的贡献是「产品定义 + 场景选择 + 交互设计 + 工程集成与验证」，不是「发明新算法」。作品集措辞据此，真实且自信，不夸大为算法创新。
- **相关技术领域锚点**(供检索，不为对标竞争)：在线时间序列预测(OTSF)、概念漂移(concept drift)、动态加权、稀疏化 / 资源自适应推理、因果归因(causal attribution)与事件影响分析。

---

## 7. 协作须知(给 GPT 及任何协作 AI)

1. **以本文件为准。** 与本文件冲突的任何临时想法，提示作者并回到本文件的边界。
2. **主动防膨胀。** 一旦作者流露出「把逻辑复杂化 / 提前冲第五阶段 / 把想法全做完 / 滑向发明新算法 / 追求商业化」的倾向，明确指出这是范围膨胀或越界，帮助拉回。这是第一优先。
3. **先交付，再上限。** 优先推动「离终点最近的那块」收尾(当前很可能是 Nestor Insight 补忽略值)，而非追求完美或深度。
4. **守架构原则。** 在设计上主动检查「能力 vs 层」是否被混淆、通用机制是否被重复实现、新模块是否偷偷修改了旧逻辑。
5. **诚实措辞。** 涉及作品集/简历表述，坚持诚实定位，不协助夸大。
6. **尊重作者主导。** 核心技术判断与创意方向由作者主导；协作 AI 的角色是照亮选项、指出代价、把关严谨性与边界，而非替作者做核心设计决策。

---

## 8. 当前状态与下一步

- **已明确：** 目标(作品集)、结构(三作品)、架构原则、收敛纪律、边界。
- **下一步(单一焦点)：** 钉死**第一个模块的边界**，它的输入、输出、**它明确不管什么**(决定能否独立)，以及「怎么算调好了」。第一个模块很可能是那个被反复复用的**通用权重机制**，或直接从 **Nestor Insight 的忽略值**切入。
- **一次只推进一个焦点。** 完成并记录后，再回到本文件更新状态、决定下一个焦点。

