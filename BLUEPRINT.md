# Nestor Delta · 蓝图与规则文档

> **本文件是 Nestor Delta repo 的唯一权威事实来源(single source of truth)。**
> 作者会定期清理对话窗口。任何一次对话中出现的方向、想法、建议，若与本文件冲突，**一律以本文件为准**。
> 需要改变方向时，先更新本文件，再据此行动，而不是让某次对话里的临时灵感覆盖既定边界。
> 协作 AI(GPT 等)在每次协作开始时应先读本文件，再读 `HANDOFF.md`，并在文末「协作须知」的约束下工作。

---

## 0. 一句话定义

Nestor Delta 是 Nestor 系列中的**数据层模块**，用于分析多变量时间序列中的关系权重、关系变化与可忽略弱信号。其最终目的**不是商业产品**，而是**一个可验证、可复现、可写进作品集的独立工程项目**。

---

## 1. 目标与定位(边界的根基)

- **本 repo 只处理 Nestor Delta(数据层)。** Nestor Insight(信息层)与完整版 Nestor 是另外的独立交付，不在本 repo 推进。
- **最终目的是「可验证的作品集」，不是「打赢市场的产品」。** 作品集证明的是「作者能否把这件事做出来、做扎实、讲明白」，不是「是否独一无二」或「能否商业化」。本项目不以收费、To B 销售或 To C 增长为目标。
- 作者的真实资产是「独立把 AI 产品从想法做到上线」的执行力。本项目服务于强化这个形象，用于欧洲 AI Product Manager / 数据产品方向求职，并替换简历中一段低价值的无关工作经历。
- **想法(idea)没有护城河，执行(execution)才有价值。** 不为「思路被看到/被借鉴」焦虑；把精力投在做出来和验证上。
- SaaS 外形只服务于作品集可信度：允许做**邀请码访问**、线上 demo、报告历史和只读分享，但这些是访问控制与展示能力，不是商业 SaaS 的计费、组织、租户或增长系统。
- Delta 后期允许通过拉取实时数据来验证实际价值。实时数据用于个人分析和作品集展示；对外能力声明仍必须区分 exploratory live run 与 frozen evidence。

---

## 2. Nestor 系列关系

三个作品可独立交付，不是必须跑到底才算数的连续阶段。

| 代号 | 层 | 内容 | 是否属于本 repo |
|------|-----|------|----------------|
| **Nestor Delta** | 数据层 | 多变量之间的关系、权重、动态变化、忽略值等的建模与预测 | ✅ 是 |
| **Nestor Insight** | 信息层 | 事件影响度评估分析 | ❌ 否 |
| **Nestor** | 完整版 | 数据层 × 信息层结合；实现「重大变化可回溯到已评估事件」的因果追溯 | ❌ 否 |

本 repo 的可交付重点是 Nestor Delta。不要把 Insight 的收尾工作、完整版 Nestor 的因果追溯，或跨层整合提前带入当前工程。

Nestor Insight 与 Delta 共享新的作品集方向：优先证明实际分析价值和工程完成度，不追求收费产品或完整商业 SaaS。若未来需要访问控制，默认从邀请码式轻量访问开始，而不是账户/组织/计费系统。

---

## 3. Delta 的里程碑边界

| 里程碑 | 名称 | 内容 | 是否可交付点 |
|--------|------|------|--------------|
| **M0** | 地基 | 评测协议 + 数据 + baseline，让一切可测、可复现 | 内部里程碑 |
| **M1** | MVP 核心 | 通用权重机制 + 三变量预测(Stage 1) + 静态信任度门控(S3.1) | ✅ 第一个可写进简历的完整作品 |
| **M2** | 深度/差异化 | 动态权重漂移(S4) + 忽略值/资源自适应(S5) | ✅ 增强版作品 |
| **M3** | 关系可信度 | S7 transformed scoring + S8 evaluation power + S9 lifecycle + S10 evidence/confidence | ✅ 已完成 |
| **W0-W5** | 展示与摄入 | report contract + FastAPI adapter + Eurostat snapshot + Streamlit UI | ✅ 本地完成 |
| — | 星辰大海 | 无边界的大规模导入、动态二级分组与跨层推理 | ❌ 不在当前范围 |

**关键规则：做完 M1 就可以停下来交付。M2 是加分项，不是义务。**

---

## 4. 架构原则(不可违背)

1. **模块化、低耦合、高内聚。** 拒绝「所有代码糊成一锅粥、牵一发动全身」。
2. **开闭原则：加新模块不改旧逻辑。** 新东西通过「组合已有模块」实现，而非修改已有模块内部。
3. **区分「能力」与「层」这两个正交维度，这是本架构的核心洞察：**
   - **能力**(通用、与具体层无关)：算关系权重 / 算忽略值 / 让权重动态变化。
   - **层**(调用能力的场景)：数据层 / 信息层 / 跨层。
   - 本 repo 当前只落地数据层，但能力模块仍要尽量设计成可复用机制，避免绑死在某个 demo 场景里。
4. **第一个要做透的，是被复用最多的底座模块**：通用权重机制。动态变化与忽略值都排在它之后。
5. **Evidence Gate 的合法输入边界：** relationship uncertainty / stability / sample support / effect size 可以进入 selection；prediction error / validation error / test error **禁止**反向进入 selection。后者会形成自激式过拟合回路：预测错 → 放宽阈值 → 模型容量增加 → 训练误差下降 → 误判为改善。

---

## 5. 收敛纪律(防膨胀，最重要的一节)

作者反复出现的最大风险是**范围膨胀 + 无限期不收尾**。以下规则用于对抗它：

1. **一次只推进一个 Sprint。** 当前 Sprint 写在 `HANDOFF.md`，不允许同时推进多个模块。
2. **只允许「容量扩展」，禁止「逻辑膨胀」。** 后期只增加摄入的数据量 / 信息量，不再把已优化的核心逻辑复杂化。
3. **不追求把所有权重模块全做完。** 作品集只需做透两三个核心，并把架构设计成其余可按同一模式扩展，无需真的全部实现。
4. **每一块交付都要有明确的「完成定义」和评估标准。** 尤其是预测/判断准不准、如何量化、达到什么程度算做成。没有终点线的模块不许开工。
5. 每冒出一个新想法，先问：**这是当前 Sprint，还是星辰大海？** 不是当前 Sprint 的东西，记录，不展开。

---

## 6. SDD 纪律：探索自由，证据冻结

Delta 的实际价值要高于叙事价值。SDD 的目的不是限制开发期调试算法，而是防止未经冻结验证的探索结果被包装成产品证据。

1. **开发期允许高频探索。** 可以频繁调参、改算法、跑对照、看中间指标、引入临时脚本、试失败方向。只要这些结果不进入 README、demo claim、里程碑验收、部署验收或简历叙事，就不需要套用完整冻结验收。
2. **证据期必须冻结验证。** 一旦某个结果要对外展示、写入作品集、作为 milestone claim、支撑 demo 或进入部署验收，必须固定数据、参数、版本、运行入口和验收命令，并记录失败/无效样本。
3. **探索日志不是产品证据。** 开发记录可以写「方向看起来有希望」；对外叙事只能写「在这些冻结条件下通过」。两者必须在文档语言上明确区分。
4. **禁止选择性诚实。** 不得因为测试结果不利而删除 fixture、改口径、只展示成功样本、把调参集当测试集，或把一次 lucky run 写成稳定能力。
5. **测试规则的作用是封版，不是刹车。** 完整测试、冻结 fixture、报告 SHA 和线上验收用于保护已声明能力；它们不应阻止开发期对算法进行频繁调试和快速迭代。
6. **实时数据可以进入探索路径。** 后期允许 live data intake 用于发现价值和演示新鲜度；但实时结果若要成为证据，必须被转化为可复现 snapshot，并写明来源、时间、hash、参数和版本。

一句话：**开发阶段最大化探索速度；验证阶段最大化可复现性；对外叙事只引用验证阶段结果。**

### 6.1 措辞边界：关系可靠性 ≠ 信息真实性(不可绕过)

Delta 验证的是**变量之间关系的可靠性与证据充分性**——这个相关性是真信号还是噪声。
它**不验证任何输入信息本身的真实性**，不做事实核查，不识别虚假信息。

因此，涉及后期实时数据的一切对外表述(README / demo 讲解 / 简历 / 面试口径)必须满足：

1. 不得出现「验证信息真实性」「判断消息真假」或任何可被如此理解的说法。
2. 输入数据的来源可信度是**前提**，由数据源选择保证，不是 Delta 的产出。
3. 正确的说法是：「在这些冻结条件下，该关系通过了证据门控」——
   而不是「该信息已验证为真」。

**为什么写死这一条：** 二者在中文口语里极易混同，而一旦被追问「你如何判断信息真实性」却答不出，
本项目全部关于诚实与严谨的建设会被一次性反噬。这是 §9.5「诚实措辞」在实时数据方向上的具体化。

---

## 7. 星辰大海(明确划出边界并合上)

以下属于远期可能，**没有独立 Sprint 边界前不投入实现**：

- 无边界的大规模导入；当前仅支持受约束的 Eurostat exact-definition intake。
- 用户切换观测视角。
- 动态二级关系分组。
- 提纯核心算法逻辑并扩展到更多领域 / 场景。
- 商业 SaaS 能力：收费、套餐、组织空间、复杂租户隔离、增长漏斗、企业级管理台。
- 任何超出 Nestor Delta 当前里程碑范围的通用化、平台化设想。

**开关规则：** 每当想给项目增加东西时，自问一句：**「这是当前交付，还是星辰大海？」** 若是后者，合上，记入 `HANDOFF.md` 的待决事项或本节，不在当下展开。

---

## 8. 技术取向(避免走弯路的默认判断)

- **不默认核心一定是 LLM。** 数据层的预测内核，很可能用经典时间序列 / 统计学习方法又快又准又省算力；LLM 更适合放在外围，例如结果解释、自然语言报告、对话式切换观测视角。
- **贡献定位诚实：** 作者的贡献是「产品定义 + 场景选择 + 交互设计 + 工程集成与验证」，不是「发明新算法」。作品集措辞据此，真实且自信，不夸大为算法创新。
- **相关技术领域锚点**(供检索，不为对标竞争)：在线时间序列预测(OTSF)、概念漂移(concept drift)、动态加权、稀疏化 / 资源自适应推理、因果归因(causal attribution)与事件影响分析。

## 9. 协作须知(给 GPT 及任何协作 AI)

1. **以本文件为准。** 与本文件冲突的任何临时想法，提示作者并回到本文件的边界。
2. **主动防膨胀。** 一旦作者流露出「把逻辑复杂化 / 提前冲远期阶段 / 把想法全做完 / 滑向发明新算法 / 追求商业化」的倾向，明确指出这是范围膨胀或越界，帮助拉回。这是第一优先。
3. **先交付，再上限。** 优先推动当前 Sprint 收尾，而非追求完美或深度。
4. **守架构原则。** 在设计上主动检查「能力 vs 层」是否被混淆、通用机制是否被重复实现、新模块是否偷偷修改了旧逻辑。
5. **诚实措辞。** 涉及作品集/简历表述，坚持诚实定位，不协助夸大。
6. **尊重作者主导。** 核心技术判断与创意方向由作者主导；协作 AI 的角色是照亮选项、指出代价、把关严谨性与边界，而非替作者做核心设计决策。

---

## 10. 当前状态与下一步

- **已明确：** 本 repo 只做 Nestor Delta；S1-S10 与 Website W0-W5 已完成，当前是可本地运行的作品集。
- **S3.1 路线细化：** 静态、OLS 前的信任度门控用于验证关系权重数值会实际改变预测；门控后的源先合成为共享关系信号，避免无约束 OLS 抵消独立特征缩放。S3.1 不回改 Sprint 3，也不包含动态权重。
- **已完成序列：** S1-S6 建立预测、门控、漂移与真实案例；S7-S10 建立 transformed scoring、evaluation power、lifecycle 与 Evidence Gate/Prediction Confidence；W0-W5 增加薄 API、Eurostat snapshot 和展示层。
- **当前焦点与下一步：** 以 `HANDOFF.md` 为准。下一阶段优先提高 Delta 的实际分析价值，而非叙事复杂度；不得默认引入收费、完整账号体系、组织租户或跨层 Insight 业务。若需要访问控制，优先做邀请码式轻量 gate。
